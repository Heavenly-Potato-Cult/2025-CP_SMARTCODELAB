#include <iostream>
using namespace std;

int main() {
    int a = 5;
    int b = 10;
    int sum = a + b;
    int diff = a - b;
    int product = a * b;
    double quotient = a / b;
    
    if (sum > 0 && diff < 20) {
    }
    
    return 0;
}